# 项目协作规范

## 开发方式

- 本项目使用 VS Code、微信开发者工具、uni-app、Vue 3 和 TypeScript，不依赖 HBuilderX。
- 按 PRD 和实施计划逐个模块交付，不一次生成大而全的功能。
- 每一项新需求先在 `docs/prd/` 留下中文 PRD，再进入开发。
- 文件夹和代码文件使用英文名称，产品文档使用中文。

## Vue 文件顺序

所有 `.vue` 文件必须严格使用以下顺序：

1. `<template>` 放在最上面。
2. `<script setup lang="ts">` 放在中间。
3. `<style scoped>` 放在最下面。

不要使用其他排列方式。

## 代码注释

- 所有新增或修改的代码都必须带有中文注释，说明文件职责、关键状态、主要流程和异常处理原因。
- 每个状态文件、服务文件、页面和组件都要在核心代码附近写注释，不能只在文件开头写一句笼统说明。
- 公共函数和容易误解的判断必须说明输入、结果或为什么这样处理。
- 样式要按页面区域分组添加注释，测试要说明所保护的业务场景。
- 注释必须解释意图和限制，不重复翻译代码本身；代码变化后要同步更新注释。

## 页面与样式

- 使用 Vue 3 Composition API 和 `<script setup lang="ts">`。
- 项目统一使用 Wot UI v2；需要通用界面组件时，必须优先检查并使用 Wot UI，不能在已有对应组件时自行编写或改用 uni-app 基础组件。
- 只有 Wot UI 未提供、无法满足明确交互需求，或属于本项目独有的品牌展示和业务组合时，才允许自定义组件，并在代码注释中说明原因。
- 小程序页面优先使用 uni-app 组件和接口，避免只能在浏览器中工作的写法。
- 颜色、圆角和间距优先复用 `src/uni.scss` 中的品牌变量。
- Logo 使用 `src/static/brand/logo.png`，视觉规范以 `docs/brand/visual-standard.md` 为准。
- 页面要包含加载、失败、空状态和重复点击保护，不能只做正常状态。

## 数据与安全

- 用户身份、家庭归属和邀请有效性由微信云端确认，前端传来的用户编号或家庭编号不能作为可信依据。
- AppSecret、云环境管理凭据和其他密钥不得写入前端、文档或版本库。
- 用户和家庭数据默认只能通过云函数访问，不允许小程序直接读写数据库。

## 目录职责

- `src/store/` 只存放 Pinia 实例和状态模块；状态模块只包含 `state`、`getters`、`actions` 以及必要的模块类型。
- 只被单个页面使用的组件必须放在该页面目录下的 `components/` 中，例如登录页私有组件放在 `src/pages/login/components/`；不要放入全局组件目录。
- `src/components/` 只存放被多个页面或业务模块共同使用的全局组件；组件提升到此目录前，必须确认存在真实的跨页面复用场景。
- 可以被多个模块使用的通用方法必须放在 `src/utils/`，不能定义在 store、页面或组件内部。
- 本地存储、格式转换、参数校验等无业务状态的方法优先放入 `src/utils/`，按用途拆分文件。
- 只属于单一业务服务的远程调用放在 `src/services/`，不要与通用工具或 Pinia 状态混放。

## 小程序分包与体积

- 主包只保留启动必需页面和公共基础能力，包括登录页、首页入口、全局状态、全局组件及必要的品牌资源。
- 非启动必需的完整业务模块应按业务域放入分包，例如事项、家庭管理、邀请和设置；同一业务域的页面、私有组件及私有资源放在同一个分包目录内。
- 分包页面不得反向引用主包页面目录中的私有组件；需要跨分包复用时，才将组件提升到 `src/components/`。
- 图片等静态资源按使用范围存放：全局品牌资源放在 `src/static/`，只被某个分包使用的资源放在对应分包内，避免被计入主包。
- 引入第三方组件时必须按需使用，禁止一次性全量注册；Wot UI 继续通过 easycom 按实际使用组件打包。
- 每次新增页面、较大图片或第三方依赖后，都要检查微信开发者工具的代码依赖分析和包体积；主包接近 1.5 MB 时提前拆分，不等到 2 MB 上限才处理。

## 交付检查

- 修改完成后至少运行类型检查和微信小程序构建。
- 页面改动必须实际打开预览，检查排版、按钮、勾选、加载和失败反馈。
- 有测试的模块必须运行对应测试；失败后修复并重新验证，不能带着已知错误交付。
- 不修改与当前需求无关的文件，不提交构建产物。
