# 我们的足迹：本轮验证记录

日期：2026-09-03。状态：进行中，尚未达到可发布标准。

## 完成标准

- 首页卡片和底部入口通向同一家足迹；地图、全部时间列表和同地点历史一致。
- 保存失败不生成重复或残缺记录，双方修改冲突不静默覆盖，成员变化后旧数据不回流。
- 照片只在完整重绘与安全检查后保存，无主文件可追踪清理。
- 本地全部检查通过；微信预览、两账号和真机授权流程实际通过后才确认完成。

## 本轮完成的补强

- 新增与编辑的重试凭证保持稳定；同一请求并发到达只提交一次。
- 在记录提交事务中重新检查家庭成员和照片状态；删除读取最新照片集合。
- 时间列表使用稳定排序游标；地点历史有独立查询和翻页，地图会继续读取后续分页。
- 首页单独读取摘要，并显示最近游玩日期；地图和列表请求可分别失败、分别重试。
- 所有状态回执核对家庭上下文；换家后不接收旧列表、详情和照片地址。
- 照片提前登记完整文件编号，预约额度原子校验，审核重试复用结果，清理先占用状态再删文件。
- 修正真实微信画布导出方式，云端检查 JPEG 段，拒绝 EXIF 等元信息。
- 保存全过程防重复点击，失败照片单独标示；草稿返回使用微信原生提示；编辑冲突显示最新内容并保留当前草稿。

## 已有验证证据

- 已通过样式检查、类型检查、全量单元测试、微信构建和包体积检查；最新一次完整运行结果将在本文件末尾补记。
- 新增跨层事务测试覆盖：并发重复新增、提交前被移出家庭、清理与照片关联互斥、删除最新照片集合、并发预约上限、非法分页游标。
- 状态测试覆盖：换家后的旧请求、上下文不归零、地图失败不影响列表、地图多页、同地点翻页、旧照片地址丢弃。
- 照片测试使用真实 8×8 JPEG，验证格式段和注入 EXIF 后的拒绝；另验证真实画布重绘和 JPEG 临时文件导出调用链。

## 尚未通过的外部验收

- 微信开发者工具自动化已能连接并发出切页请求，但页面数据和截图请求反复超时。独立窗口截图显示小程序停在未勾选协议的登录页；未替用户勾选协议或登录。不能把工具启动、切页回执或登录页截图算作足迹页面验收。
- 云端只读列表确认足迹及清理函数存在，但目前均为 3 秒超时；需确认照片审核和清理所需超时、集合、索引、存储权限，以及本地源码部署一致性。本轮未覆盖部署云函数或变更线上权限。
- 仍需实际完成双入口、3 张照片保存、共同编辑/删除、成员移除、真机位置拒绝与恢复、草稿返回、500 个地点性能及真实云文件清理。
- 尚未完成最终完整代码审查与 PRD 逐条签收；不得标记为已交付或可发布。

## 继续条件

用户在当前项目的微信开发者工具重新编译并完成测试小程序登录后，恢复页面操作验证；同时在云开发控制台确认发布配置。用户已明确要求继续使用当前分支并提交、推送，不创建新分支。

## 最新运行结果

- `pnpm run verify:mp-weixin`：退出码 0；样式、类型、全部测试、微信构建和包体积均通过。
- 全量单元测试复跑：56 组、713 项全部通过。既有家庭测试仍输出类目初始化模拟依赖缺失日志，测试结果通过；这是原有测试夹具问题，本轮未扩大范围修改。
- 主包 795.6 KB；足迹分包 32.1 KB；其它分包均低于项目 1.5 MB 警戒线。
- 显式启用的微信入口自动测试：1 项失败，原因为“读取首页足迹卡片超时”。不是跳过，也不是通过；尚未观察到足迹页面完成真实渲染与操作。
- `git diff --check` 通过；当前分支仍为 `codex/feat-login-entry-flow`，未创建新分支、提交或推送。

## 用户反馈白屏后的排查

- 用户截图显示首页卡片加载时提示 `utils/footprint-display.js is not defined`，发生在页面初始化阶段，不能归因于云函数未上传。
- 检查构建目录：该文件存在，首页卡片的相对引用路径正确。重新构建成功；检查全部 251 处相对 `require` 引用，没有缺失目标。
- 仅清理当前项目的编译缓存、重新建立文件索引并重新打开项目，未清理用户存储或登录信息；没有为此改动业务代码。缓存或文件索引失效仍是待确认的判断，不能标记修复完成。
- 重新打开后，窗口出现“您信任此项目的作者吗？”确认框。等待用户自行点击“信任并运行”，未代替用户确认，也未绕过提示。确认后还需检查白屏是否消失及实际页面操作。
- 本次未上传云函数；仍需同步 `footprint`、`cleanup-footprint-data`、`household` 并核验配套配置。

## 页面恢复后的云端修复

- 用户确认页面恢复。窗口截图已实际看到首页“我们的足迹”卡片，以及足迹页的四个底部入口；同时看到足迹数据加载失败，不能据此认为功能已完成。
- 已下载三个云函数到忽略目录 `tmp/footprint-preview/cloud-backup-20260903/` 备份。对比全部顶层业务 JS、JSON 文件（排除依赖锁文件），修复前 `footprint`、`cleanup-footprint-data`、`household` 均与本地一致，无需盲目全量重传。
- 从实际 `wx-server-sdk` 实现确认：聚合 `end()` 返回 `list`，普通 `get()` 返回 `data`。足迹摘要和地点分页误读 `data`，会分别抛出读取 `0`、`slice` 的异常。已修正两处读取。
- 新增四项回归测试覆盖空摘要、地点计数、空地点列表、地点分页。修复前四项全部失败，修复后全部通过。
- 已通过开发者工具增量上传 `footprint/repository-data.js`，上传结果成功（1 个文件，4.3 KB）。未更改家庭业务文件、数据权限、超时或清理触发器。
- 修复后再次运行 `pnpm run verify:mp-weixin` 退出码 0；主包 795.6 KB、足迹分包 32.1 KB。
- 全量复跑为 56 组、717 项通过；`git diff --check` 通过。重新下载已部署的 `repository-data.js`，SHA-256 与本地完全相同，确认云端更新已落地。
- 自动化连接改用独立端口后可读到首页路径，但元素读取与云端调用检查仍超时，没有取得有效业务回执。不能用路径回执证明点击或云端读取成功。
- 尚需用户刷新足迹页确认实际加载结果；云端足迹与清理函数仍为 3 秒超时，照片相关配置、集合索引及访问权限仍未完成核验。

## 零足迹仍保留地图（用户指定的小范围修复）

- 用户后续日志确认缺少 `footprintEntries` 集合；用户报告已创建所需集合，并提供了正常显示“还没有共同足迹”的页面截图。空数据读取已恢复，但这不代表索引、权限、照片保存等验收完成。
- 本次只修改足迹主页：大空态卡片仅出现在列表模式，地图模式即使零记录也保留原生地图；首次记录提示与原有 Wot 按钮放在地图下方，不覆盖地图交互区域。不改隐私配置或云函数。
- 新增真实模板编译回归测试，覆盖零记录地图、不显示当前位置、首次记录按钮、列表空态、已有地点及真实地图失败反馈。原实现出现两个预期失败，修复后 5 项全部通过；连同原足迹视图测试共 7 项通过。
- `pnpm run verify:mp-weixin` 完整通过，主包 796.4 KB、足迹分包 32.1 KB；`git diff --check` 通过。构建模板中已确认地图与其下方引导为独立节点。
- 通过微信自动化接口尝试读取预览截图，12 秒超时，未取得截图；未声称实际地图瓦片、拖动或缩放已验证。本轮没有新开分支、提交或上传云端文件。

## 地点导航与页面重设计

- 新增独立地点导航：可以先选择目的地再打开微信地图，也可以从已选中的足迹和足迹详情直接导航；打开导航不会新增或修改足迹。
- 足迹首页改为旅行手账式层级，地图继续作为主内容；零记录引导固定在地图下方，列表空状态缩短，底部导航保留安全间距。
- 最新完整检查通过：58 组、728 项单元测试全部通过，微信小程序构建成功；主包 805.3 KB，足迹分包 32.4 KB，其它分包也均低于项目警戒线。
- 构建后的页面结构已确认包含顶部概览、独立导航、地图容器、零记录引导和列表标题。由于自动化无法读取微信开发者工具页面，最终视觉与真机导航跳转仍需在用户本机确认。
